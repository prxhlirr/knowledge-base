# 历史数据迁移方案 v1.2

## 1. 本版本目标

本方案是 `历史数据迁移方案v1.1.md` 的辅助索引专项修订版。

当前离线环境已按 v1.1 完成其他索引迁移，剩余问题集中在：

1. `kb_doc_search_v1` / `kb_doc_meta` 的 mapping 与当前项目期望 mapping 不一致。
2. `kb_doc_search_v1` 中可检索内容存在 URL percent-encoding 污染，例如 `%E9%9D%99...`。
3. 主文档迁移完成后，关键词检索结果为空。
4. 需要保证重建后的 `kb_doc_search` / `kb_doc_meta` 与 `kb_document*` 内容一一对应。

本方案目标：

- 不原地修改旧 `kb_doc_search_v1` / `kb_doc_meta`。
- 从已迁移完成的 v2 chunk 索引重建干净辅助索引。
- 将污染旧索引降级为只读审计与回滚材料，不再作为正式检索数据源。
- 用可执行命令完成离线环境排查、重建、校验、权限覆盖、alias 切换与回滚。

## 2. 第一性原理分析

### 2.1 mapping 不一致不能原地修复

ES 字段 mapping 一旦创建，核心字段类型不可原地修改。例如 `text`、`keyword`、`dense_vector`、多字段 analyzer 等结构变化，不能靠 `put_mapping` 把旧字段变成当前项目期望结构。

因此：

- 旧 `kb_doc_meta` 不应继续作为当前相似度检索索引。
- 旧 `kb_doc_search_v1` 如果 mapping 与当前项目不一致，不应继续作为关键词检索索引。
- 正确方式是新建目标索引，用当前项目 mapping 写入干净数据。

### 2.2 URL 编码污染不能作为中文检索内容

关键词检索依赖 ES analyzer 对中文内容建立倒排索引。如果 `source`、`title`、`doc_terms`、`summary` 中存储的是 `%E9%9D%99%E5%AE%81...`，ES 看到的是百分号、字母和数字，而不是中文词。

因此：

- 污染数据会导致中文关键词无法命中。
- 不能只切 alias 到污染索引。
- 禁止在旧索引上 update-by-query 直接清洗，因为旧 mapping 已不可信，且清洗过程无法稳定保证与 v2 chunk 一一对应。

### 2.3 辅助索引的一一对应必须以 v2 chunk 为事实源

`kb_doc_search` 和 `kb_doc_meta` 都是文档级辅助索引，它们不能自己成为内容事实源。

唯一可信事实源应是已经迁移完成的 v2 chunk 索引：

- `kb_document_v1_v2`
- `kb_document_law_v2`
- 其他已经确认迁移完成的 `kb_document_*_v2` 物理索引

一一对应原则：

- 以 v2 chunk 中 latest 文档的 `metadata.source` 集合作为基准。
- `kb_doc_search` 每个 `source` 只允许一条 `is_latest=true` 文档级记录。
- `kb_doc_meta` 每个 `source` 只允许一条 `is_latest=true` 文档级向量记录。
- 允许没有 fine vector 的文档缺少 `kb_doc_meta`，但必须输出缺失清单并人工确认。
- 不允许 `kb_doc_search` 缺少可检索字段。

## 3. 处理决策

### 3.1 `kb_doc_search_v1` 污染数据处理

决策：不在旧 `kb_doc_search_v1` 上原地清洗，不删除旧索引。

处理方式：

1. 备份旧 mapping、settings、alias、样本数据。
2. 将旧 `kb_doc_search_v1` 从正式读写 alias 中摘除。
3. 新建 `kb_doc_search_v2`。
4. 从 v2 chunk 重建 `kb_doc_search_v2`。
5. 校验通过后，将 `kb_doc_search` / `kb_doc_search_write` alias 切到 `kb_doc_search_v2`。
6. 旧 `kb_doc_search_v1` 保留只读观察期，用于审计和回滚。

### 3.2 `kb_doc_meta` mapping 不一致处理

决策：不复用旧 `kb_doc_meta`，统一使用当前项目期望的 `kb_doc_meta_v2`。

处理方式：

1. 备份旧 `kb_doc_meta`。
2. 确认或新建 `kb_doc_meta_v2`。
3. 从 v2 chunk 的 fine chunk vector 均值池化生成 `doc_vector`。
4. 将 `kb_doc_meta_read` / `kb_doc_meta_write` alias 指向 `kb_doc_meta_v2`。
5. 旧 `kb_doc_meta` 保留只读观察期。

### 3.3 权限处理

辅助索引重建时不得信任旧辅助索引权限。

正确流程：

1. 重建阶段允许写入保守默认 `_NO_ACCESS` 或从 chunk 投影来的临时权限。
2. 重建完成后，必须用 MySQL 权限快照或 Java 权限投影覆盖：
   - `kb_doc_meta_v2`
   - `kb_doc_search_v2`
3. 权限覆盖完成前，不允许将真实业务检索流量切到新辅助索引。

## 4. 离线执行前环境变量

以下命令在服务器 A 或具备 Docker 权限的迁移机执行。

```bash
export MIGRATION_BATCH_ID="offline-es-aux-v12-$(date +%Y%m%d%H%M%S)"
export AI_CONTAINER="<AI_DOCKER_CONTAINER_NAME>"
export ES_HOST="http://<ES_HOST>:9200"
export ES_USER="<ES_USER>"
export ES_PASS="<ES_PASS>"
export REPORT_DIR="/tmp/kb_migration_reports/$MIGRATION_BATCH_ID"

export SOURCE_INDEX="kb_document_v1_v2,kb_document_law_v2"
export KB_DOC_META_INDEX="kb_doc_meta_v2"
export KB_DOC_SEARCH_INDEX="kb_doc_search_v2"
export KB_DOC_META_DEFAULT_ACL_TOKENS="_NO_ACCESS"
```

如果离线 ES 使用 HTTPS，将 `ES_HOST` 改为：

```bash
export ES_HOST="https://<ES_HOST>:9200"
```

创建报告目录：

```bash
docker exec \
  -e REPORT_DIR="$REPORT_DIR" \
  "$AI_CONTAINER" \
  sh -lc 'mkdir -p "$REPORT_DIR" && test -w "$REPORT_DIR"'
```

确认 AI 容器脚本存在：

```bash
docker exec "$AI_CONTAINER" sh -lc 'test -f /app/scripts/backfill_doc_meta_v2.py && echo "backfill_doc_meta_v2.py OK"'
docker exec "$AI_CONTAINER" sh -lc 'test -f /app/scripts/backfill_kb_doc_search.py && echo "backfill_kb_doc_search.py OK"'
docker exec "$AI_CONTAINER" sh -lc 'test -f /app/scripts/apply_mysql_permission_snapshot.py && echo "apply_mysql_permission_snapshot.py OK"'
```

## 5. 当前离线 Docker 入库链路代码证据

本节只依据当前项目代码得出结论，用于确认新入库数据是否进入修复后的 alias 链路。

### 5.1 Docker 启动入口

当前 `ai_service/Dockerfile` 的启动命令为：

```text
CMD ["python", "-m", "uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8001", "--workers", "1"]
```

证据文件：`ai_service/Dockerfile`

```text
COPY main.py task_worker.py task_worker_qa.py ./
COPY core/ ./core/
COPY scripts/ ./scripts/
CMD ["python", "-m", "uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8001", "--workers", "1"]
```

结论：

- Docker 主进程加载 `main.py`。
- 当前镜像同时包含 `core/` 与 `scripts/`，但 Docker CMD 没有直接运行 `scripts/rag_pipeline.py`。

### 5.2 Worker 是否消费入库任务

`main.py` 在满足以下任一条件时启动 Redis 文档入库 Worker：

- `AI_START_WORKER=true`
- `AI_CAPABILITIES` 包含 `worker`

证据文件：`ai_service/main.py`

```text
from task_worker import main as run_worker

if _env_bool("AI_START_WORKER", False) or _has_capability("worker"):
    threading.Thread(target=_worker_watchdog, daemon=True, name="task-worker-watchdog").start()
```

结论：

- 离线容器如果配置为纯 API 实例，不消费入库任务。
- 离线容器如果配置了 `AI_START_WORKER=true` 或 `AI_CAPABILITIES=worker/all`，消费入库任务的入口是 `task_worker.py`。

容器内确认命令：

```bash
docker exec "$AI_CONTAINER" sh -lc 'echo "AI_START_WORKER=$AI_START_WORKER"; echo "AI_CAPABILITIES=$AI_CAPABILITIES"'
docker logs "$AI_CONTAINER" 2>&1 | grep -E "Starting Redis document worker watchdog|RAGPipeline 初始化成功|AI_START_WORKER=false" | tail -n 50
```

### 5.3 当前 Worker 调用的是 `core.rag_pipeline.RAGPipeline`

证据文件：`ai_service/task_worker.py`

```text
from core.rag_pipeline import RAGPipeline

stats = pipeline.process_and_index(
    file_path,
    original_name=original_name,
    ext_metadata=ext_meta
)
```

结论：

- 当前 Worker 入库链路调用 `core.rag_pipeline.RAGPipeline`。
- 当前 Worker 没有导入 `scripts/rag_pipeline.py`。

容器内确认命令：

```bash
docker exec "$AI_CONTAINER" sh -lc 'cd /app && python - <<'"'"'PY'"'"'
import inspect
import task_worker
from core.rag_pipeline import RAGPipeline
print("task_worker_file=", inspect.getfile(task_worker))
print("RAGPipeline_file=", inspect.getfile(RAGPipeline))
PY'
```

期望输出包含：

```text
task_worker_file= /app/task_worker.py
RAGPipeline_file= /app/core/rag_pipeline.py
```

### 5.4 当前 `core.rag_pipeline` 使用 `DocIndexer` 写辅助索引

证据文件：`ai_service/core/rag_pipeline.py`

```text
from core.indexing.doc_indexer import DocIndexer

self.doc_indexer = DocIndexer(self.es)

self.doc_indexer.update_doc_meta(...)
self.doc_indexer.update_doc_search(...)
```

证据文件：`ai_service/core/indexing/doc_indexer.py`

```text
from core.indexing.es_setup import DOC_META_WRITE_ALIAS, DOC_SEARCH_WRITE_ALIAS

self._index_with_optional_fields(DOC_META_WRITE_ALIAS, meta_es_id, body, [])
self._index_with_optional_fields(DOC_SEARCH_WRITE_ALIAS, search_es_id, body, [])
```

结论：

- 当前新入库辅助索引写入点是 alias：
  - `kb_doc_meta_write`
  - `kb_doc_search_write`
- alias 切到 `kb_doc_meta_v2` / `kb_doc_search_v2` 后，当前 Worker 新入库数据进入目标辅助索引。

### 5.5 旧脚本风险边界

仓库中仍存在旧脚本：`ai_service/scripts/rag_pipeline.py`。

该脚本中存在硬编码：

```text
DOC_META_INDEX = "kb_doc_meta"
```

但当前 Docker CMD 和 `task_worker.py` 不调用该脚本。

结论：

- 当前 Docker 服务入库链路不走旧 `scripts/rag_pipeline.py`。
- 如果现场人工执行 `python /app/scripts/rag_pipeline.py`，会绕过当前 Worker 链路并写向旧 `kb_doc_meta`。
- 离线迁移期间禁止人工使用 `scripts/rag_pipeline.py` 执行入库。

### 5.6 新入库链路最终判定

当前代码层面的确定结论：

1. Docker 服务启动入口是 `main.py`。
2. 文档入库 Worker 入口是 `task_worker.py`。
3. `task_worker.py` 调用 `core.rag_pipeline.RAGPipeline`。
4. `core.rag_pipeline.RAGPipeline` 委托 `DocIndexer` 更新辅助索引。
5. `DocIndexer` 使用 `kb_doc_meta_write` 和 `kb_doc_search_write` 写入辅助索引。

因此，迁移完成后只要 alias 状态为：

```text
kb_doc_meta_write   -> kb_doc_meta_v2
kb_doc_search_write -> kb_doc_search_v2
```

当前 Docker Worker 新入库数据会写入目标辅助索引。

## 6. 只读审计

### 6.1 备份 alias、mapping、settings

```bash
curl -u "$ES_USER:$ES_PASS" "$ES_HOST/_cat/aliases/kb_doc*?v" \
  > "aliases_kb_doc_before_aux_fix_$MIGRATION_BATCH_ID.txt"

curl -u "$ES_USER:$ES_PASS" "$ES_HOST/kb_doc_search_v1/_mapping?pretty" \
  > "mapping_kb_doc_search_v1_before_aux_fix_$MIGRATION_BATCH_ID.json"

curl -u "$ES_USER:$ES_PASS" "$ES_HOST/kb_doc_search_v1/_settings?pretty" \
  > "settings_kb_doc_search_v1_before_aux_fix_$MIGRATION_BATCH_ID.json"

curl -u "$ES_USER:$ES_PASS" "$ES_HOST/kb_doc_meta/_mapping?pretty" \
  > "mapping_kb_doc_meta_before_aux_fix_$MIGRATION_BATCH_ID.json"

curl -u "$ES_USER:$ES_PASS" "$ES_HOST/kb_doc_meta/_settings?pretty" \
  > "settings_kb_doc_meta_before_aux_fix_$MIGRATION_BATCH_ID.json"
```

如果 `kb_doc_meta_v2` 已存在，也备份：

```bash
curl -u "$ES_USER:$ES_PASS" "$ES_HOST/kb_doc_meta_v2/_mapping?pretty" \
  > "mapping_kb_doc_meta_v2_before_aux_fix_$MIGRATION_BATCH_ID.json"
```

### 6.2 确认当前 alias 指向

```bash
curl -u "$ES_USER:$ES_PASS" "$ES_HOST/_cat/aliases/kb_doc_search,kb_doc_search_write,kb_doc_meta_read,kb_doc_meta_write?v"
```

重点确认：

- `kb_doc_search` 是否仍指向污染的 `kb_doc_search_v1`。
- `kb_doc_search_write` 是否仍指向污染的 `kb_doc_search_v1`。
- `kb_doc_meta_read` / `kb_doc_meta_write` 是否指向旧 `kb_doc_meta`。

### 6.3 抽样确认 URL 编码污染

```bash
curl -s -u "$ES_USER:$ES_PASS" \
  -H 'Content-Type: application/json' \
  "$ES_HOST/kb_doc_search_v1/_search?pretty" \
  -d '{
    "size": 20,
    "_source": ["source", "title", "doc_terms", "summary", "is_latest", "acl_tokens"],
    "query": {"match_all": {}}
  }'
```

如果 `source`、`title`、`doc_terms`、`summary` 中大量出现 `%E`、`%e`、`%25`，判定该索引内容污染，不得继续作为正式检索索引。

### 6.4 统计疑似污染比例

```bash
curl -s -u "$ES_USER:$ES_PASS" \
  -H 'Content-Type: application/json' \
  "$ES_HOST/kb_doc_search_v1/_count?pretty" \
  -d '{
    "query": {
      "bool": {
        "should": [
          {"wildcard": {"source": "*%E*"}},
          {"wildcard": {"title.keyword": "*%E*"}},
          {"wildcard": {"doc_terms": "*%E*"}},
          {"wildcard": {"summary": "*%E*"}}
        ],
        "minimum_should_match": 1
      }
    }
  }'
```

注意：如果旧 mapping 没有 `title.keyword`，该命令会报错。报错本身也说明旧 mapping 与当前项目不兼容，继续走重建流程。

### 6.5 确认 v2 chunk 内容正常

```bash
curl -s -u "$ES_USER:$ES_PASS" \
  -H 'Content-Type: application/json' \
  "$ES_HOST/$SOURCE_INDEX/_search?pretty" \
  -d '{
    "size": 5,
    "_source": ["content", "display_content", "metadata.source", "metadata.title", "metadata.is_latest", "metadata.doc_id", "metadata.content_hash"],
    "query": {"match_all": {}}
  }'
```

确认样本中：

- `content` 或 `display_content` 是正常中文。
- `metadata.source` 存在。
- `metadata.title` 尽量存在，不存在时允许用 `metadata.source` 兜底。

如果 `content` 或 `display_content` 中存在大量乱码，本方案停止进入第 7 步。

根因判定：

- `kb_doc_search_v2` 和 `kb_doc_meta_v2` 都从 `kb_document*_v2` 聚合生成。
- `ai_service/scripts/backfill_kb_doc_search.py` 的 `new_group()` / `add_chunk()` 读取 `src.get("display_content") or src.get("content")`。
- `ai_service/scripts/backfill_doc_meta_v2.py` 的 `new_group()` 读取 `src.get("content")` 生成 `summary`。
- `core.normalization.text_normalizer.normalize_content_if_fully_encoded()` 只修复整段 URL percent-encoding，不修复普通字符集乱码。

因此，`kb_document*_v2.content` / `display_content` 已经污染时，重建辅助索引会把污染内容投影到 `doc_terms`、`summary` 和相似度摘要字段中。此时不能把 v2 chunk 作为干净事实源。

### 6.6 统计 v2 chunk 正文乱码类型

先区分污染类型。不同类型的处理方式不同，不能混在同一条修复命令里处理。

```bash
docker exec \
  -e ES_HOST="$ES_HOST" \
  -e ES_USER="$ES_USER" \
  -e ES_PASS="$ES_PASS" \
  -e SOURCE_INDEX="$SOURCE_INDEX" \
  -e REPORT_DIR="$REPORT_DIR" \
  "$AI_CONTAINER" \
  sh -lc 'python -X utf8 - <<'"'"'PY'"'"'
import base64
import json
import os
import re
import urllib.parse
import urllib.request

ES_HOST = os.environ["ES_HOST"].rstrip("/")
ES_USER = os.getenv("ES_USER", "")
ES_PASS = os.getenv("ES_PASS", "")
SOURCE_INDEX = os.environ["SOURCE_INDEX"]
REPORT_DIR = os.environ["REPORT_DIR"]

AUTH = ""
if ES_USER or ES_PASS:
    AUTH = "Basic " + base64.b64encode(f"{ES_USER}:{ES_PASS}".encode()).decode()

HEADERS = {"Content-Type": "application/json"}
if AUTH:
    HEADERS["Authorization"] = AUTH

URL_ENCODED_RE = re.compile(r"(?i)%[0-9a-f]{2}")
REPLACEMENT_RE = re.compile(r"\ufffd")
MOJIBAKE_RE = re.compile(r"[ÃÂâäåæçèéêëìíîïðñòóôõöùúûüýþÿ]")
CJK_RE = re.compile(r"[\u3400-\u9fff]")

def req(path, body):
    data = json.dumps(body, ensure_ascii=False).encode("utf-8")
    request = urllib.request.Request(
        ES_HOST + path,
        data=data,
        headers=HEADERS,
        method="POST",
    )
    with urllib.request.urlopen(request, timeout=120) as resp:
        return json.loads(resp.read().decode("utf-8"))

body = {
    "size": 500,
    "_source": ["content", "display_content", "metadata.source", "metadata.chunk_id", "metadata.is_latest"],
    "query": {"term": {"metadata.is_latest": True}},
}

resp = req(f"/{urllib.parse.quote(SOURCE_INDEX, safe='*,_')}/_search", body)
rows = []
stats = {
    "sample_total": 0,
    "url_encoded": 0,
    "replacement_char": 0,
    "mojibake_like": 0,
    "low_cjk_ratio": 0,
}

for hit in resp.get("hits", {}).get("hits", []):
    src = hit.get("_source") or {}
    meta = src.get("metadata") or {}
    text = str(src.get("display_content") or src.get("content") or "")
    if not text:
        continue
    stats["sample_total"] += 1
    cjk = len(CJK_RE.findall(text))
    ratio = cjk / max(len(text), 1)
    flags = []
    if URL_ENCODED_RE.search(text):
        stats["url_encoded"] += 1
        flags.append("url_encoded")
    if REPLACEMENT_RE.search(text):
        stats["replacement_char"] += 1
        flags.append("replacement_char")
    if MOJIBAKE_RE.search(text):
        stats["mojibake_like"] += 1
        flags.append("mojibake_like")
    if ratio < 0.05 and len(text) >= 80:
        stats["low_cjk_ratio"] += 1
        flags.append("low_cjk_ratio")
    if flags:
        rows.append({
            "_index": hit.get("_index"),
            "_id": hit.get("_id"),
            "source": meta.get("source"),
            "chunk_id": meta.get("chunk_id"),
            "flags": flags,
            "preview": text[:300],
        })

os.makedirs(REPORT_DIR, exist_ok=True)
report_path = os.path.join(REPORT_DIR, "kb_document_v2_content_pollution_report.json")
with open(report_path, "w", encoding="utf-8") as f:
    json.dump({"stats": stats, "samples": rows[:100]}, f, ensure_ascii=False, indent=2)

print(json.dumps(stats, ensure_ascii=False, indent=2))
print("report:", report_path)
PY'
```

判定规则：

- `url_encoded > 0`：正文存在 URL percent-encoding 污染。
- `mojibake_like > 0`：正文存在字符集错解污染，例如 UTF-8 字节被按 Latin-1 或 GBK 显示。
- `replacement_char > 0`：正文已经出现 U+FFFD 替换字符，ES 内文本不可逆，必须回到原始文件重新解析。
- `low_cjk_ratio > 0`：中文文档正文中文占比异常低，需要结合样本人工确认。

### 6.7 v2 chunk 正文污染处理决策

处理顺序固定为：

1. 先停止第 7 步到第 15 步，不创建、不重建、不切 alias。
2. 导出 `kb_document_v2_content_pollution_report.json`。
3. 抽样确认原始文件是否正常。
4. 按污染类型选择修复路径。

污染类型与修复路径：

- 仅 URL percent-encoding：可以基于 ES 中现有字段做确定性解码，但解码后正文发生变化，向量字段与正文不再一致；完成正文修复后必须重新向量化受影响 chunk。
- 字符集错解且没有 U+FFFD：只有在样本可通过固定编码反转恢复中文时，才允许批量修复；修复后必须重新向量化受影响 chunk。
- 含 U+FFFD 替换字符：ES 内信息已经丢失，不能靠 ES 原值修复；必须从原始文件重新解析、重新分块、重新向量化、重新写入 `kb_document*_v2`。
- 原始文件本身乱码：迁移不能修复内容事实源，只能输出异常清单，由数据侧替换原始文件后重新入库。

当前代码证据：

- `ai_service/core/rag_pipeline.py` 已包含解析净化、乱码行过滤、质量门控和 chunk `display_content` 写入逻辑。
- `ai_service/scripts/migrate_kb_document_index_v2.py` 是历史 chunk 迁移脚本，只投影已有 `content/display_content`，不重新解析原始文件。
- `ai_service/scripts/backfill_kb_doc_search.py` 与 `ai_service/scripts/backfill_doc_meta_v2.py` 只消费 v2 chunk，不具备从原始文件重新解析正文的职责。

因此，发现 v2 chunk 正文乱码后，正确事实源从 `kb_document*_v2` 上移到“原始文件 + 当前 `core.rag_pipeline.py` 入库链路”。历史迁移链路只能用于结构迁移，不能生成新的干净正文。

## 7. 新建目标辅助索引

### 7.1 禁止直接覆盖旧索引

本方案不对以下索引执行 delete 或原地 update-by-query 清洗：

- `kb_doc_search_v1`
- `kb_doc_meta`

原因：

- 旧 mapping 不可信。
- 污染数据清洗无法保证完整恢复业务语义。
- 原地修改会破坏回滚和审计依据。

### 7.2 目标索引命名

本次辅助索引修复目标为：

| 用途 | 目标物理索引 | 读 alias | 写 alias |
|---|---|---|---|
| 文档级关键词预筛 | `kb_doc_search_v2` | `kb_doc_search` | `kb_doc_search_write` |
| 文档级相似度向量 | `kb_doc_meta_v2` | `kb_doc_meta_read` | `kb_doc_meta_write` |

如果离线环境已有其他命名约定，可以改目标物理索引名，但 alias 名称保持当前项目默认值。

### 7.3 用当前项目标准 mapping 显式创建目标索引

当前项目的标准 mapping 入口在 `ai_service/core/indexing/es_setup.py`：

- `doc_meta_index_mapping()`
- `doc_search_index_mapping()`

代码证据：

```text
doc_meta_index_mapping() 包含 source_index、index_code、owner_unit_code、visible_unit_codes、permission_version、doc_vector HNSW 配置。
doc_search_index_mapping() 包含 source.text、source.ngram、title.ngram、doc_terms、summary、权限字段和 source_index。
```

执行原则：

- 目标索引不存在时，先用标准 mapping 显式创建。
- 不让 `backfill_doc_meta_v2.py` 自动创建 `kb_doc_meta_v2`。
- `backfill_kb_doc_search.py` 的 mapping 与 `es_setup.py` 中 doc search mapping 对齐，但仍统一采用显式创建，减少现场差异。

创建 `kb_doc_meta_v2` 与 `kb_doc_search_v2`：

```bash
docker exec \
  -e ES_HOST="$ES_HOST" \
  -e ES_USER="$ES_USER" \
  -e ES_PASS="$ES_PASS" \
  -e KB_DOC_META_INDEX="kb_doc_meta_v2" \
  -e KB_DOC_SEARCH_INDEX="kb_doc_search_v2" \
  "$AI_CONTAINER" \
  sh -lc 'cd /app && python -X utf8 - <<'"'"'PY'"'"'
import base64
import json
import os
import urllib.error
import urllib.request

from core.indexing.es_setup import doc_meta_index_mapping, doc_search_index_mapping

ES_HOST = os.environ["ES_HOST"].rstrip("/")
ES_USER = os.environ.get("ES_USER", "")
ES_PASS = os.environ.get("ES_PASS", "")
DOC_META_INDEX = os.environ.get("KB_DOC_META_INDEX", "kb_doc_meta_v2")
DOC_SEARCH_INDEX = os.environ.get("KB_DOC_SEARCH_INDEX", "kb_doc_search_v2")

def req(method, path, body=None):
    data = json.dumps(body).encode("utf-8") if body is not None else None
    headers = {"Content-Type": "application/json"}
    if ES_USER:
        token = base64.b64encode(f"{ES_USER}:{ES_PASS}".encode("utf-8")).decode("ascii")
        headers["Authorization"] = f"Basic {token}"
    request = urllib.request.Request(ES_HOST + path, data=data, headers=headers, method=method)
    try:
        with urllib.request.urlopen(request, timeout=120) as resp:
            raw = resp.read().decode("utf-8")
            return json.loads(raw) if raw else {}
    except urllib.error.HTTPError as exc:
        if method == "HEAD" and exc.code == 404:
            return None
        raw = exc.read().decode("utf-8")
        raise RuntimeError(f"{method} {path} failed: {exc.code} {raw}")

def exists(index):
    return req("HEAD", f"/{index}") is not None

if not exists(DOC_META_INDEX):
    req("PUT", f"/{DOC_META_INDEX}", doc_meta_index_mapping())
    print(f"created {DOC_META_INDEX} with project standard mapping")
else:
    print(f"{DOC_META_INDEX} exists, skip create")

if not exists(DOC_SEARCH_INDEX):
    req("PUT", f"/{DOC_SEARCH_INDEX}", doc_search_index_mapping())
    print(f"created {DOC_SEARCH_INDEX} with project standard mapping")
else:
    print(f"{DOC_SEARCH_INDEX} exists, skip create")
PY'
```

创建后校验关键 mapping 字段：

```bash
curl -s -u "$ES_USER:$ES_PASS" "$ES_HOST/kb_doc_meta_v2/_mapping?pretty" | grep -E '"doc_vector"|"source_index"|"permission_version"'
curl -s -u "$ES_USER:$ES_PASS" "$ES_HOST/kb_doc_search_v2/_mapping?pretty" | grep -E '"doc_terms"|"source"|"ngram"|"source_index"|"permission_version"'
```

## 8. 重建 `kb_doc_meta_v2`

`kb_doc_meta_v2` 从 v2 chunk 的 fine chunk vector 均值池化生成，不从旧 `kb_doc_meta` 复制。

执行：

```bash
docker exec \
  -e ES_HOST="$ES_HOST" \
  -e ES_USER="$ES_USER" \
  -e ES_PASS="$ES_PASS" \
  -e SOURCE_INDEX="$SOURCE_INDEX" \
  -e KB_DOC_META_INDEX="kb_doc_meta_v2" \
  -e KB_DOC_META_READ_ALIAS="kb_doc_meta_v2_read_tmp" \
  -e KB_DOC_META_WRITE_ALIAS="kb_doc_meta_v2_write_tmp" \
  -e KB_DOC_META_DEFAULT_ACL_TOKENS="$KB_DOC_META_DEFAULT_ACL_TOKENS" \
  "$AI_CONTAINER" \
  sh -lc 'cd /app && python -X utf8 /app/scripts/backfill_doc_meta_v2.py'
```

说明：

- 临时 alias 使用 `kb_doc_meta_v2_read_tmp` / `kb_doc_meta_v2_write_tmp`，避免脚本执行期间提前影响 Java 服务。
- 正式 alias 在校验通过后单独切换。

## 9. 重建 `kb_doc_search_v2`

`kb_doc_search_v2` 从 v2 chunk 聚合生成文档级关键词记录，不从旧 `kb_doc_search_v1` 复制。

执行：

```bash
docker exec \
  -e ES_HOST="$ES_HOST" \
  -e ES_USER="$ES_USER" \
  -e ES_PASS="$ES_PASS" \
  -e SOURCE_INDEX="$SOURCE_INDEX" \
  -e KB_DOC_SEARCH_INDEX="kb_doc_search_v2" \
  -e KB_DOC_SEARCH_READ_ALIAS="kb_doc_search_v2_read_tmp" \
  -e KB_DOC_SEARCH_WRITE_ALIAS="kb_doc_search_v2_write_tmp" \
  -e KB_DOC_META_DEFAULT_ACL_TOKENS="$KB_DOC_META_DEFAULT_ACL_TOKENS" \
  -e BACKFILL_MODE="full" \
  "$AI_CONTAINER" \
  sh -lc 'cd /app && python -X utf8 /app/scripts/backfill_kb_doc_search.py'
```

执行后刷新：

```bash
curl -X POST -u "$ES_USER:$ES_PASS" "$ES_HOST/kb_doc_search_v2/_refresh"
curl -X POST -u "$ES_USER:$ES_PASS" "$ES_HOST/kb_doc_meta_v2/_refresh"
```

## 10. 一一对应校验

### 10.1 统计 v2 chunk latest source 数

```bash
curl -s -u "$ES_USER:$ES_PASS" \
  -H 'Content-Type: application/json' \
  "$ES_HOST/$SOURCE_INDEX/_search?pretty" \
  -d '{
    "size": 0,
    "query": {
      "bool": {
        "must_not": [{"term": {"chunk_granularity": "coarse"}}],
        "filter": [
          {
            "bool": {
              "should": [
                {"term": {"metadata.is_latest": true}},
                {"bool": {"must_not": {"exists": {"field": "metadata.is_latest"}}}}
              ],
              "minimum_should_match": 1
            }
          },
          {"exists": {"field": "metadata.source"}}
        ]
      }
    },
    "aggs": {
      "source_count": {
        "cardinality": {"field": "metadata.source", "precision_threshold": 40000}
      }
    }
  }'
```

### 10.2 统计 `kb_doc_search_v2` source 数

```bash
curl -s -u "$ES_USER:$ES_PASS" \
  -H 'Content-Type: application/json' \
  "$ES_HOST/kb_doc_search_v2/_search?pretty" \
  -d '{
    "size": 0,
    "query": {"term": {"is_latest": true}},
    "aggs": {
      "source_count": {
        "cardinality": {"field": "source", "precision_threshold": 40000}
      }
    }
  }'
```

### 10.3 统计 `kb_doc_meta_v2` source 数

```bash
curl -s -u "$ES_USER:$ES_PASS" \
  -H 'Content-Type: application/json' \
  "$ES_HOST/kb_doc_meta_v2/_search?pretty" \
  -d '{
    "size": 0,
    "query": {"term": {"is_latest": true}},
    "aggs": {
      "source_count": {
        "cardinality": {"field": "source", "precision_threshold": 40000}
      }
    }
  }'
```

验收判断：

- `kb_doc_search_v2.source_count` 必须等于 v2 chunk latest source 数。
- `kb_doc_meta_v2.source_count` 原则上应等于 v2 chunk latest source 数。
- 如果部分文档没有 fine vector，`kb_doc_meta_v2` 会少于 chunk source 数；此时必须输出缺失清单并人工确认。

### 10.4 检查 `kb_doc_search_v2` 重复 source

```bash
curl -s -u "$ES_USER:$ES_PASS" \
  -H 'Content-Type: application/json' \
  "$ES_HOST/kb_doc_search_v2/_search?pretty" \
  -d '{
    "size": 0,
    "query": {"term": {"is_latest": true}},
    "aggs": {
      "dup_sources": {
        "terms": {
          "field": "source",
          "min_doc_count": 2,
          "size": 100
        }
      }
    }
  }'
```

期望：`dup_sources.buckets` 为空。

### 10.5 检查 `kb_doc_meta_v2` 重复 source

```bash
curl -s -u "$ES_USER:$ES_PASS" \
  -H 'Content-Type: application/json' \
  "$ES_HOST/kb_doc_meta_v2/_search?pretty" \
  -d '{
    "size": 0,
    "query": {"term": {"is_latest": true}},
    "aggs": {
      "dup_sources": {
        "terms": {
          "field": "source",
          "min_doc_count": 2,
          "size": 100
        }
      }
    }
  }'
```

期望：`dup_sources.buckets` 为空。

### 10.6 导出辅助索引缺失 source 清单

以下命令使用容器内 Python 对比 source 集合，输出缺失和多余清单。

```bash
docker exec \
  -e ES_HOST="$ES_HOST" \
  -e ES_USER="$ES_USER" \
  -e ES_PASS="$ES_PASS" \
  -e SOURCE_INDEX="$SOURCE_INDEX" \
  -e REPORT_DIR="$REPORT_DIR" \
  "$AI_CONTAINER" \
  sh -lc 'cd /app && python -X utf8 - <<'"'"'PY'"'"'
import base64, json, os, urllib.request

ES_HOST = os.environ["ES_HOST"].rstrip("/")
ES_USER = os.environ.get("ES_USER", "")
ES_PASS = os.environ.get("ES_PASS", "")
SOURCE_INDEX = os.environ["SOURCE_INDEX"]
REPORT_DIR = os.environ["REPORT_DIR"]

def req(method, path, body=None):
    data = json.dumps(body).encode("utf-8") if body is not None else None
    headers = {"Content-Type": "application/json"}
    if ES_USER:
        token = base64.b64encode(f"{ES_USER}:{ES_PASS}".encode()).decode()
        headers["Authorization"] = f"Basic {token}"
    request = urllib.request.Request(ES_HOST + path, data=data, headers=headers, method=method)
    with urllib.request.urlopen(request, timeout=180) as resp:
        raw = resp.read().decode("utf-8")
        return json.loads(raw) if raw else {}

def composite_sources(index, field, query):
    result = set()
    after = None
    while True:
        body = {
            "size": 0,
            "query": query,
            "aggs": {
                "sources": {
                    "composite": {
                        "size": 5000,
                        "sources": [{"source": {"terms": {"field": field}}}]
                    }
                }
            }
        }
        if after:
            body["aggs"]["sources"]["composite"]["after"] = after
        resp = req("POST", f"/{index}/_search", body)
        agg = resp.get("aggregations", {}).get("sources", {})
        for bucket in agg.get("buckets", []):
            value = bucket.get("key", {}).get("source")
            if value:
                result.add(value)
        after = agg.get("after_key")
        if not after:
            return result

chunk_query = {
    "bool": {
        "must_not": [{"term": {"chunk_granularity": "coarse"}}],
        "filter": [
            {"exists": {"field": "metadata.source"}},
            {
                "bool": {
                    "should": [
                        {"term": {"metadata.is_latest": True}},
                        {"bool": {"must_not": {"exists": {"field": "metadata.is_latest"}}}}
                    ],
                    "minimum_should_match": 1
                }
            }
        ]
    }
}
aux_query = {"term": {"is_latest": True}}

chunk_sources = composite_sources(SOURCE_INDEX, "metadata.source", chunk_query)
search_sources = composite_sources("kb_doc_search_v2", "source", aux_query)
meta_sources = composite_sources("kb_doc_meta_v2", "source", aux_query)

report = {
    "chunk_source_count": len(chunk_sources),
    "doc_search_source_count": len(search_sources),
    "doc_meta_source_count": len(meta_sources),
    "missing_in_doc_search": sorted(chunk_sources - search_sources),
    "extra_in_doc_search": sorted(search_sources - chunk_sources),
    "missing_in_doc_meta": sorted(chunk_sources - meta_sources),
    "extra_in_doc_meta": sorted(meta_sources - chunk_sources),
}

os.makedirs(REPORT_DIR, exist_ok=True)
path = os.path.join(REPORT_DIR, "auxiliary_source_parity_report.json")
with open(path, "w", encoding="utf-8") as f:
    json.dump(report, f, ensure_ascii=False, indent=2)
print(json.dumps({k: (len(v) if isinstance(v, list) else v) for k, v in report.items()}, ensure_ascii=False, indent=2))
print(path)
PY'
```

验收要求：

- `missing_in_doc_search` 必须为空。
- `extra_in_doc_search` 必须为空。
- `extra_in_doc_meta` 必须为空。
- `missing_in_doc_meta` 如果不为空，必须逐条确认是否因为源文档没有 fine vector。

## 11. 内容质量校验

### 11.1 检查 `kb_doc_search_v2` 可检索字段非空

```bash
for field in source title doc_terms summary; do
  echo "field=$field empty/missing"
  curl -s -u "$ES_USER:$ES_PASS" \
    -H 'Content-Type: application/json' \
    "$ES_HOST/kb_doc_search_v2/_count?pretty" \
    -d "{\"query\":{\"bool\":{\"should\":[{\"bool\":{\"must_not\":{\"exists\":{\"field\":\"$field\"}}}},{\"term\":{\"$field\":\"\"}}],\"minimum_should_match\":1}}}"
done
```

### 11.2 检查 URL 编码污染是否消失

```bash
curl -s -u "$ES_USER:$ES_PASS" \
  -H 'Content-Type: application/json' \
  "$ES_HOST/kb_doc_search_v2/_search?pretty" \
  -d '{
    "size": 20,
    "_source": ["source", "title", "doc_terms", "summary"],
    "query": {
      "bool": {
        "should": [
          {"wildcard": {"source": "*%E*"}},
          {"wildcard": {"title.keyword": "*%E*"}},
          {"wildcard": {"doc_terms": "*%E*"}},
          {"wildcard": {"summary": "*%E*"}}
        ],
        "minimum_should_match": 1
      }
    }
  }'
```

期望：返回 0 条或仅返回业务内容中真实包含 `%E` 的极少数异常样本。

### 11.3 关键词直查冒烟

```bash
curl -s -u "$ES_USER:$ES_PASS" \
  -H 'Content-Type: application/json' \
  "$ES_HOST/kb_doc_search_v2/_search?pretty" \
  -d '{
    "size": 10,
    "_source": ["source", "title", "summary", "acl_tokens"],
    "query": {
      "multi_match": {
        "query": "静宁苹果",
        "fields": ["title^4", "source.text^3", "doc_terms^2", "summary"]
      }
    }
  }'
```

如果 v2 chunk 中确实存在“静宁苹果”相关内容，该查询应返回命中文档。

## 12. 权限覆盖

重建辅助索引后，必须重新覆盖权限。不得因为辅助索引刚从 chunk 重建，就跳过 MySQL 权限权威覆盖。

如果已经按 v1.1 导出了 MySQL 权限快照：

```bash
docker cp "$PERMISSION_SNAPSHOT" "$AI_CONTAINER":/tmp/mysql_permission_snapshot.jsonl
docker exec "$AI_CONTAINER" sh -lc 'test -s /tmp/mysql_permission_snapshot.jsonl'
```

dry-run：

```bash
docker exec \
  -e ES_HOST="$ES_HOST" \
  -e ES_USER="$ES_USER" \
  -e ES_PASS="$ES_PASS" \
  -e REPORT_DIR="$REPORT_DIR" \
  "$AI_CONTAINER" \
  sh -lc 'cd /app && python -X utf8 /app/scripts/apply_mysql_permission_snapshot.py \
  --input /tmp/mysql_permission_snapshot.jsonl \
  --chunk-index kb_document \
  --aux-indexes kb_doc_meta_v2,kb_doc_search_v2,kb_qa_pairs_v2 \
  --limit 1000 \
  --sample 20 \
  --output "$REPORT_DIR/mysql_permission_snapshot_aux_dry_run.json"'
```

确认 dry-run 报告后执行：

```bash
docker exec \
  -e ES_HOST="$ES_HOST" \
  -e ES_USER="$ES_USER" \
  -e ES_PASS="$ES_PASS" \
  -e REPORT_DIR="$REPORT_DIR" \
  "$AI_CONTAINER" \
  sh -lc 'cd /app && python -X utf8 /app/scripts/apply_mysql_permission_snapshot.py \
  --input /tmp/mysql_permission_snapshot.jsonl \
  --chunk-index kb_document \
  --aux-indexes kb_doc_meta_v2,kb_doc_search_v2,kb_qa_pairs_v2 \
  --sample 50 \
  --execute \
  --output "$REPORT_DIR/mysql_permission_snapshot_aux_execute.json"'
```

权限校验：

```bash
for index in kb_doc_meta_v2 kb_doc_search_v2; do
  echo "index=$index acl_tokens missing"
  curl -s -u "$ES_USER:$ES_PASS" \
    -H 'Content-Type: application/json' \
    "$ES_HOST/$index/_count?pretty" \
    -d '{"query":{"bool":{"must_not":[{"exists":{"field":"acl_tokens"}}]}}}'

  echo "index=$index _NO_ACCESS count"
  curl -s -u "$ES_USER:$ES_PASS" \
    -H 'Content-Type: application/json' \
    "$ES_HOST/$index/_count?pretty" \
    -d '{"query":{"term":{"acl_tokens":"_NO_ACCESS"}}}'
done
```

## 13. alias 切换

切换前再次备份：

```bash
curl -u "$ES_USER:$ES_PASS" "$ES_HOST/_cat/aliases/kb_doc*?v" \
  > "aliases_before_aux_alias_switch_$MIGRATION_BATCH_ID.txt"
```

切换 `kb_doc_search`：

```bash
curl -X POST -u "$ES_USER:$ES_PASS" \
  -H 'Content-Type: application/json' \
  "$ES_HOST/_aliases" \
  -d '{
    "actions": [
      {"remove": {"index": "*", "alias": "kb_doc_search"}},
      {"remove": {"index": "*", "alias": "kb_doc_search_write"}},
      {"add": {"index": "kb_doc_search_v2", "alias": "kb_doc_search"}},
      {"add": {"index": "kb_doc_search_v2", "alias": "kb_doc_search_write", "is_write_index": true}}
    ]
  }'
```

切换 `kb_doc_meta`：

```bash
curl -X POST -u "$ES_USER:$ES_PASS" \
  -H 'Content-Type: application/json' \
  "$ES_HOST/_aliases" \
  -d '{
    "actions": [
      {"remove": {"index": "*", "alias": "kb_doc_meta_read"}},
      {"remove": {"index": "*", "alias": "kb_doc_meta_write"}},
      {"add": {"index": "kb_doc_meta_v2", "alias": "kb_doc_meta_read"}},
      {"add": {"index": "kb_doc_meta_v2", "alias": "kb_doc_meta_write", "is_write_index": true}}
    ]
  }'
```

切换后确认：

```bash
curl -u "$ES_USER:$ES_PASS" "$ES_HOST/_cat/aliases/kb_doc*?v"
```

## 14. 新入库链路验证

本节用于验证迁移后的新文档入库确实进入目标辅助索引。

### 14.1 切换后确认写 alias

```bash
curl -u "$ES_USER:$ES_PASS" "$ES_HOST/_cat/aliases/kb_doc_search_write,kb_doc_meta_write?v"
```

期望：

```text
kb_doc_search_write -> kb_doc_search_v2
kb_doc_meta_write   -> kb_doc_meta_v2
```

### 14.2 确认 Worker 运行的是当前链路

```bash
docker exec "$AI_CONTAINER" sh -lc 'cd /app && python - <<'"'"'PY'"'"'
import inspect
import task_worker
from core.rag_pipeline import RAGPipeline
from core.indexing.doc_indexer import DocIndexer
print("task_worker=", inspect.getfile(task_worker))
print("RAGPipeline=", inspect.getfile(RAGPipeline))
print("DocIndexer=", inspect.getfile(DocIndexer))
PY'
```

期望：

```text
task_worker= /app/task_worker.py
RAGPipeline= /app/core/rag_pipeline.py
DocIndexer= /app/core/indexing/doc_indexer.py
```

### 14.3 新入库前记录旧索引计数

```bash
curl -s -u "$ES_USER:$ES_PASS" "$ES_HOST/kb_doc_search_v1/_count?pretty"
curl -s -u "$ES_USER:$ES_PASS" "$ES_HOST/kb_doc_meta/_count?pretty"
curl -s -u "$ES_USER:$ES_PASS" "$ES_HOST/kb_doc_search_v2/_count?pretty"
curl -s -u "$ES_USER:$ES_PASS" "$ES_HOST/kb_doc_meta_v2/_count?pretty"
```

### 14.4 上传一篇测试文档后按 source 验证

将 `<NEW_SOURCE_NAME>` 替换为新入库文档的真实 `source`。

```bash
export NEW_SOURCE_NAME="<NEW_SOURCE_NAME>"
```

验证新 chunk：

```bash
curl -s -u "$ES_USER:$ES_PASS" \
  -H 'Content-Type: application/json' \
  "$ES_HOST/$SOURCE_INDEX/_search?pretty" \
  -d "{
    \"size\": 5,
    \"_source\": [\"metadata.source\", \"metadata.title\", \"chunk_granularity\"],
    \"query\": {\"term\": {\"metadata.source\": \"$NEW_SOURCE_NAME\"}}
  }"
```

验证新 `kb_doc_search_v2`：

```bash
curl -s -u "$ES_USER:$ES_PASS" \
  -H 'Content-Type: application/json' \
  "$ES_HOST/kb_doc_search_v2/_search?pretty" \
  -d "{
    \"size\": 5,
    \"_source\": [\"source\", \"title\", \"doc_terms\", \"summary\", \"is_latest\", \"acl_tokens\"],
    \"query\": {\"term\": {\"source\": \"$NEW_SOURCE_NAME\"}}
  }"
```

验证新 `kb_doc_meta_v2`：

```bash
curl -s -u "$ES_USER:$ES_PASS" \
  -H 'Content-Type: application/json' \
  "$ES_HOST/kb_doc_meta_v2/_search?pretty" \
  -d "{
    \"size\": 5,
    \"_source\": [\"source\", \"title\", \"chunk_count\", \"is_latest\", \"acl_tokens\"],
    \"query\": {\"term\": {\"source\": \"$NEW_SOURCE_NAME\"}}
  }"
```

验证旧辅助索引没有新 source：

```bash
curl -s -u "$ES_USER:$ES_PASS" \
  -H 'Content-Type: application/json' \
  "$ES_HOST/kb_doc_search_v1/_count?pretty" \
  -d "{\"query\":{\"term\":{\"source\":\"$NEW_SOURCE_NAME\"}}}"

curl -s -u "$ES_USER:$ES_PASS" \
  -H 'Content-Type: application/json' \
  "$ES_HOST/kb_doc_meta/_count?pretty" \
  -d "{\"query\":{\"term\":{\"source\":\"$NEW_SOURCE_NAME\"}}}"
```

验收结果：

- `kb_document*_v2` 能查到新 source 的 chunk。
- `kb_doc_search_v2` 能查到新 source 的文档级关键词记录。
- `kb_doc_meta_v2` 能查到新 source 的文档级向量记录。若源文档没有 fine vector，输出异常并人工确认。
- 旧 `kb_doc_search_v1` 对新 source count 为 0。
- 旧 `kb_doc_meta` 对新 source count 为 0。

## 15. 切换后冒烟测试

### 15.1 ES 关键词直查

```bash
curl -s -u "$ES_USER:$ES_PASS" \
  -H 'Content-Type: application/json' \
  "$ES_HOST/kb_doc_search/_search?pretty" \
  -d '{
    "size": 10,
    "_source": ["source", "title", "summary", "acl_tokens"],
    "query": {
      "multi_match": {
        "query": "静宁苹果",
        "fields": ["title^4", "source.text^3", "doc_terms^2", "summary"]
      }
    }
  }'
```

### 15.2 Java 关键词检索

```bash
curl -X POST "$JAVA_BASE_URL/api/search" \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer <USER_TOKEN>" \
  -d '{"keyword":"静宁苹果","topK":20}'
```

### 15.3 文档相似度检索

文档相似度依赖 `kb_doc_meta_read`。切换后应验证：

- `kb_doc_meta_read` 指向 `kb_doc_meta_v2`。
- `kb_doc_meta_v2.doc_vector` 存在。
- 输入与库内内容相关时，相似文档能返回结果。

ES 抽样确认 `doc_vector`：

```bash
curl -s -u "$ES_USER:$ES_PASS" \
  -H 'Content-Type: application/json' \
  "$ES_HOST/kb_doc_meta_read/_search?pretty" \
  -d '{
    "size": 3,
    "_source": ["source", "title", "chunk_count", "doc_vector", "acl_tokens"],
    "query": {"exists": {"field": "doc_vector"}}
  }'
```

## 16. 回滚方案

如果切换后关键词检索或相似度检索出现严重异常，优先回滚 alias，不删除新索引。

回滚 `kb_doc_search`：

```bash
curl -X POST -u "$ES_USER:$ES_PASS" \
  -H 'Content-Type: application/json' \
  "$ES_HOST/_aliases" \
  -d '{
    "actions": [
      {"remove": {"index": "kb_doc_search_v2", "alias": "kb_doc_search"}},
      {"remove": {"index": "kb_doc_search_v2", "alias": "kb_doc_search_write"}},
      {"add": {"index": "kb_doc_search_v1", "alias": "kb_doc_search"}},
      {"add": {"index": "kb_doc_search_v1", "alias": "kb_doc_search_write", "is_write_index": true}}
    ]
  }'
```

回滚 `kb_doc_meta`：

```bash
curl -X POST -u "$ES_USER:$ES_PASS" \
  -H 'Content-Type: application/json' \
  "$ES_HOST/_aliases" \
  -d '{
    "actions": [
      {"remove": {"index": "kb_doc_meta_v2", "alias": "kb_doc_meta_read"}},
      {"remove": {"index": "kb_doc_meta_v2", "alias": "kb_doc_meta_write"}},
      {"add": {"index": "kb_doc_meta", "alias": "kb_doc_meta_read"}},
      {"add": {"index": "kb_doc_meta", "alias": "kb_doc_meta_write", "is_write_index": true}}
    ]
  }'
```

回滚后必须：

- 保留 `kb_doc_search_v2` / `kb_doc_meta_v2` 用于排查。
- 不立即删除新索引。
- 导出 Java 检索日志、ES 查询样本和 alias 状态。
- 修正问题后重新执行校验和切换。

## 17. 验收标准

### 17.1 mapping 验收

- `kb_doc_search_v2` 使用 `ai_service/core/indexing/es_setup.py::doc_search_index_mapping()` 创建。
- `kb_doc_meta_v2` 使用 `ai_service/core/indexing/es_setup.py::doc_meta_index_mapping()` 创建。
- 旧 `kb_doc_search_v1` / `kb_doc_meta` 不再承载正式读写 alias。

### 17.2 内容验收

- `kb_doc_search_v2.source/title/doc_terms/summary` 为正常中文或正常业务文本。
- URL 编码污染样本数为 0，或仅有明确业务允许的极少数例外。
- 关键词直查能命中 v2 chunk 中确实存在的文档。

### 17.3 一一对应验收

- v2 chunk latest source 数等于 `kb_doc_search_v2` latest source 数。
- `kb_doc_search_v2` 无重复 latest source。
- `kb_doc_search_v2` 无多余 source。
- `kb_doc_meta_v2` 无重复 latest source。
- `kb_doc_meta_v2` 无多余 source。
- `kb_doc_meta_v2` 缺失 source 必须能解释为源文档没有 fine vector，并输出人工确认清单。

### 17.4 权限验收

- `kb_doc_search_v2` 和 `kb_doc_meta_v2` 均存在 `acl_tokens`。
- 权限覆盖已基于 MySQL 权限快照或 Java 权限投影执行。
- `_NO_ACCESS` 文档进入人工确认清单。
- 无权限用户无法通过关键词检索或相似度检索看到受限文档。

### 17.5 新入库链路验收

- Docker 主入口为 `main.py`。
- Worker 启用时入口为 `task_worker.py`。
- `task_worker.py` 加载 `/app/core/rag_pipeline.py`。
- `/app/core/rag_pipeline.py` 使用 `DocIndexer`。
- `DocIndexer` 写入 `kb_doc_search_write` 和 `kb_doc_meta_write`。
- 新入库测试文档只出现在 `kb_doc_search_v2` / `kb_doc_meta_v2`，不出现在旧 `kb_doc_search_v1` / `kb_doc_meta`。

### 17.6 业务验收

- Java 关键词检索不再返回空结果。
- 文档相似度检索可返回相关文档。
- QA 检索不受辅助索引切换影响。
- alias 回滚命令已验证可执行。

## 18. 明确禁止事项

- 禁止在旧 `kb_doc_search_v1` 上原地清洗 URL 编码污染后继续作为正式索引。
- 禁止在旧 `kb_doc_meta` 上原地修改 mapping。
- 禁止从旧辅助索引反推最终权限。
- 禁止权限覆盖前把新辅助索引切给真实用户检索。
- 禁止切换 alias 前不备份。
- 禁止发现检索异常后立即删除新索引。
- 禁止人工执行 `/app/scripts/rag_pipeline.py` 作为离线入库入口。

## 19. 最终执行顺序摘要

1. 备份旧 alias、mapping、settings。
2. 抽样确认 `kb_doc_search_v1` URL 编码污染。
3. 确认 v2 chunk 内容完整且中文正常。
4. 用当前项目标准 mapping 显式创建 `kb_doc_meta_v2` 和 `kb_doc_search_v2`。
5. 从 v2 chunk 重建 `kb_doc_meta_v2`。
6. 从 v2 chunk 重建 `kb_doc_search_v2`。
7. 校验 source 一一对应。
8. 校验 URL 编码污染消失。
9. 用 MySQL 权限快照覆盖辅助索引权限。
10. 执行 ES 关键词直查和相似度基础校验。
11. 切换 `kb_doc_search` / `kb_doc_meta` alias。
12. 验证 Docker Worker 运行 `task_worker.py -> core.rag_pipeline.py -> DocIndexer` 链路。
13. 上传新文档并验证新数据只进入 `kb_doc_search_v2` / `kb_doc_meta_v2`。
14. 执行 Java 关键词检索、相似度检索、无权限用户检索冒烟。
15. 保留旧索引只读观察，进入灰度验证。
