# 历史索引迁移方案

## 1. 背景与目标

当前项目已经完成文档权限控制链路改造，新入库数据会写入以下权限字段：

| 字段 | 目标类型 | 用途 |
| --- | --- | --- |
| `acl_tokens` | `keyword` | ES 前置权限过滤核心字段 |
| `source_index` | `keyword` | 角色可读索引/文档类型过滤 |
| `index_code` | `keyword` | 业务索引编码 |
| `owner_unit_code` | `keyword` | 文档归属单位 |
| `visible_unit_codes` | `keyword` | 单位可见链 |
| `permission_version` | `long` | 权限投影版本 |
| `metadata.acl_tokens` | `keyword` | 历史兼容字段 |
| `metadata.visible_unit_codes` | `keyword` | 历史兼容字段 |

但是历史 `kb_document_*` 索引存在 mapping 不一致：

- 部分索引 `acl_tokens` 是 `text + keyword`，不是纯 `keyword`。
- 部分空索引缺少根级 `acl_tokens`。
- ES 不支持把已有字段从 `text` 原地改成 `keyword`。

因此必须通过“新建 v2 索引 + scroll/bulk 迁移 + alias 原子切换”的方式完成历史索引治理。

本方案面向 **AI 服务 Docker 部署场景**，用于离线环境或生产环境下执行全量历史索引迁移。

## 2. 当前真实状态

状态采集时间：2026-07-06。

状态快照：

- `docs/test_reports/20260706_historical_index_migration_current_state.json`

### 2.1 已完成迁移

| 源索引 | v2 索引 | count | alias 状态 | 结论 |
| --- | --- | ---: | --- | --- |
| `kb_document_news` | `kb_document_news_v2` | 16 | `kb_document` 和 `kb_document_news_write` 已指向 v2 | 已完成迁移和切换 |

已完成报告：

- `docs/test_reports/20260706_kb_document_news_v2_execute_report.md`
- `docs/test_reports/20260706_kb_document_news_v2_alias_switch_report.md`

### 2.2 未完成迁移

| 源索引 | 当前 count | 当前问题 | 目标动作 |
| --- | ---: | --- | --- |
| `kb_document_public` | 22 | `acl_tokens = text + keyword` | 迁移到 `kb_document_public_v2` |
| `kb_document_official` | 669 | `acl_tokens = text + keyword` | 迁移到 `kb_document_official_v2` |
| `kb_document_law` | 0 | `acl_tokens = text + keyword` | 创建 `kb_document_law_v2` 并切 alias |
| `kb_document_notice` | 0 | 缺少根级 `acl_tokens` | 创建 `kb_document_notice_v2` 并切 alias |
| `kb_document_v1` | 0 | 缺少根级 `acl_tokens` | 创建 `kb_document_v1_v2` 并切 alias，或确认废弃后移出读 alias |

## 3. 迁移原则

### 3.1 不直接改旧索引

原因：

- ES 不允许原地把字段类型从 `text` 改为 `keyword`。
- 对旧索引做复杂动态修改容易污染现有读路径。

### 3.2 不使用纯 `_reindex`

原因：

- 需要做字段白名单投影。
- 需要补齐权限根字段和 `metadata.*` 镜像字段。
- 需要在缺失 `acl_tokens` 时 fail-closed 写入 `_NO_ACCESS`。
- 需要避免迁移 `colloquial_vector` 等冗余字段。

因此固定使用：

```text
scroll + bulk
```

### 3.3 每个索引独立迁移、独立切换

原因：

- 降低单次变更风险。
- 每个索引可独立回滚。
- 适合 Docker 离线环境分批执行。

### 3.4 切 alias 前必须冻结对应索引写入

原因：

- 迁移期间如果旧写 alias 仍有新文档写入，v2 会漏数据。
- 对于正在迁移的单个业务索引，必须暂停对应类型的入库任务或停掉 worker。

## 4. Docker 环境准备

以下命令默认在宿主机项目根目录执行。

### 4.1 确认容器名称

```bash
docker ps --format "table {{.Names}}\t{{.Image}}\t{{.Status}}"
```

下文使用变量表示 AI 服务容器名：

```bash
AI_CONTAINER=kb-ai
ES_HOST=http://elasticsearch:9200
```

如果实际容器名不同，应替换 `AI_CONTAINER`。

### 4.2 确认脚本存在

```bash
docker exec "$AI_CONTAINER" sh -c 'test -f /app/scripts/migrate_kb_document_index_v2.py && echo OK'
```

如果返回不是 `OK`，说明镜像尚未包含最新脚本，需要重新构建或挂载最新代码。

### 4.3 创建报告目录

```bash
docker exec "$AI_CONTAINER" sh -c 'mkdir -p /app/docs/test_reports'
```

### 4.4 确认 ES 连通

```bash
docker exec "$AI_CONTAINER" sh -c 'python - << "PY"
import urllib.request
print(urllib.request.urlopen("'$ES_HOST'", timeout=10).read().decode()[:300])
PY'
```

## 5. 迁移工具说明

迁移工具：

```text
/app/scripts/migrate_kb_document_index_v2.py
```

核心参数：

| 参数 | 说明 |
| --- | --- |
| `--source-index` | 源索引 |
| `--target-index` | 目标 v2 索引 |
| `--batch-size` | 每批迁移条数 |
| `--limit` | 限制迁移条数；0 表示不限制 |
| `--sample` | 打印样例数量 |
| `--create-target` | 允许创建目标索引 |
| `--execute` | 真实写入 |
| `--switch-alias` | 执行 alias 原子切换 |
| `--output` | 输出 JSON 报告 |
| `--shards` | 目标索引主分片数 |
| `--replicas` | 目标索引副本数 |

默认行为：

- 不创建索引。
- 不写入数据。
- 不切 alias。
- 不删除旧索引。

## 6. 全量迁移顺序

推荐顺序：

1. `kb_document_public`：22 条，低风险。
2. `kb_document_official`：669 条，中等风险。
3. `kb_document_law`：0 条，空索引，切换验证。
4. `kb_document_notice`：0 条，空索引，切换验证。
5. `kb_document_v1`：0 条，确认是否保留；如保留则迁移到 v2，如废弃则移出 `kb_document` alias。

`kb_document_news` 已完成，不重复执行。

## 7. 单索引标准流程

以下以变量形式描述，每个索引都执行同一套流程。

```bash
SRC=kb_document_public
DST=kb_document_public_v2
WRITE_ALIAS=kb_document_public_write
```

### 7.1 冻结写入

必须暂停对应索引的入库任务。

可选方式：

```bash
docker exec "$AI_CONTAINER" sh -c 'export AI_START_WORKER=false'
```

更稳妥做法是在部署层暂停 AI worker 或 Java 入库入口，只保留查询服务。

### 7.2 导出迁移前基线

```bash
docker exec "$AI_CONTAINER" sh -c '
mkdir -p /app/docs/test_reports
curl -s "$ES_HOST/_cat/aliases/kb_document*?h=alias,index,is_write_index&format=json" \
  > /app/docs/test_reports/${SRC}_before_aliases.json
curl -s "$ES_HOST/${SRC}/_mapping" \
  > /app/docs/test_reports/${SRC}_before_mapping.json
curl -s "$ES_HOST/${SRC}/_settings" \
  > /app/docs/test_reports/${SRC}_before_settings.json
curl -s "$ES_HOST/${SRC}/_count" \
  > /app/docs/test_reports/${SRC}_before_count.json
'
```

注意：如果容器 shell 中没有继承 `SRC/ES_HOST`，请改为显式值。

### 7.3 dry-run

```bash
docker exec "$AI_CONTAINER" sh -c '
python -X utf8 /app/scripts/migrate_kb_document_index_v2.py \
  --es-host "$ES_HOST" \
  --source-index "$SRC" \
  --target-index "$DST" \
  --batch-size 200 \
  --limit 1000 \
  --sample 20 \
  --create-target \
  --dry-run \
  --output "/app/docs/test_reports/${DST}_dry_run.json"
'
```

验收：

- `written = 0`
- `failed = 0`
- 样例中的 `acl_tokens` 不为空。
- 不应创建目标索引。

### 7.4 创建目标索引并小批量迁移

```bash
docker exec "$AI_CONTAINER" sh -c '
python -X utf8 /app/scripts/migrate_kb_document_index_v2.py \
  --es-host "$ES_HOST" \
  --source-index "$SRC" \
  --target-index "$DST" \
  --batch-size 200 \
  --limit 1000 \
  --sample 20 \
  --create-target \
  --execute \
  --output "/app/docs/test_reports/${DST}_execute_batch.json"
'
```

验收：

- `failed = 0`
- `written` 等于本轮扫描数。
- 如果源索引总量小于 1000，则此步骤已经是全量迁移。

### 7.5 全量迁移

如果源索引大于 1000 条，再执行全量迁移：

```bash
docker exec "$AI_CONTAINER" sh -c '
python -X utf8 /app/scripts/migrate_kb_document_index_v2.py \
  --es-host "$ES_HOST" \
  --source-index "$SRC" \
  --target-index "$DST" \
  --batch-size 500 \
  --limit 0 \
  --sample 20 \
  --create-target \
  --execute \
  --output "/app/docs/test_reports/${DST}_execute_full.json"
'
```

### 7.6 迁移后校验

```bash
docker exec "$AI_CONTAINER" sh -c '
python -X utf8 - << "PY"
import json, urllib.request

ES_HOST = "'$ES_HOST'"
SRC = "'$SRC'"
DST = "'$DST'"
TOKEN = "dept::620102"

def get(path, method="GET", body=None):
    data = None if body is None else json.dumps(body).encode()
    req = urllib.request.Request(
        ES_HOST + path,
        data=data,
        headers={"Content-Type": "application/json"},
        method=method,
    )
    return json.loads(urllib.request.urlopen(req, timeout=30).read().decode())

old_count = get(f"/{SRC}/_count")["count"]
new_count = get(f"/{DST}/_count")["count"]
mapping = get(f"/{DST}/_mapping")
props = mapping[DST]["mappings"]["properties"]
acl_count = get(f"/{DST}/_count", "POST", {"query": {"term": {"acl_tokens": TOKEN}}})["count"]

print(json.dumps({
    "old_count": old_count,
    "new_count": new_count,
    "acl_tokens_type": props["acl_tokens"]["type"],
    "source_index_type": props["source_index"]["type"],
    "visible_unit_codes_type": props["visible_unit_codes"]["type"],
    "permission_version_type": props["permission_version"]["type"],
    "acl_count_for_sample_token": acl_count,
}, ensure_ascii=False, indent=2))
PY'
```

验收：

- `old_count == new_count`
- `acl_tokens_type == keyword`
- `source_index_type == keyword`
- `visible_unit_codes_type == keyword`
- `permission_version_type == long`
- 抽样 token 能命中合理数量。

### 7.7 alias 切换

确认校验通过后执行：

```bash
docker exec "$AI_CONTAINER" sh -c '
python -X utf8 /app/scripts/migrate_kb_document_index_v2.py \
  --es-host "$ES_HOST" \
  --source-index "$SRC" \
  --target-index "$DST" \
  --batch-size 500 \
  --limit 0 \
  --sample 5 \
  --create-target \
  --execute \
  --switch-alias \
  --output "/app/docs/test_reports/${DST}_alias_switch.json"
'
```

验收：

- `kb_document` read alias 中，当前业务索引应指向 `DST`。
- `${WRITE_ALIAS}` write alias 应指向 `DST`，且 `is_write_index=true`。
- 旧索引不再挂对应 read/write alias。

### 7.8 alias 切换后验证

```bash
docker exec "$AI_CONTAINER" sh -c '
curl -s "$ES_HOST/_cat/aliases/kb_document*?h=alias,index,is_write_index&format=json"
curl -s "$ES_HOST/kb_document/_count" \
  -H "Content-Type: application/json" \
  -d "{\"query\":{\"term\":{\"source_index\":\"$SRC\"}}}"
'
```

验收：

- `kb_document` alias 下 `source_index=$SRC` 的数量等于源索引 count。
- 不应出现 `old + new` 双倍数量。

## 8. 各索引执行命令

### 8.1 kb_document_public

```bash
SRC=kb_document_public
DST=kb_document_public_v2
WRITE_ALIAS=kb_document_public_write
```

按第 7 章执行。

### 8.2 kb_document_official

```bash
SRC=kb_document_official
DST=kb_document_official_v2
WRITE_ALIAS=kb_document_official_write
```

按第 7 章执行。

### 8.3 kb_document_law

```bash
SRC=kb_document_law
DST=kb_document_law_v2
WRITE_ALIAS=kb_document_law_write
```

当前 count 为 0。仍建议创建 v2 并切 alias，原因是旧 mapping 中 `acl_tokens` 不是纯 `keyword`。

### 8.4 kb_document_notice

```bash
SRC=kb_document_notice
DST=kb_document_notice_v2
WRITE_ALIAS=kb_document_notice_write
```

当前 count 为 0。仍建议创建 v2 并切 alias，原因是旧 mapping 缺少根级 `acl_tokens`。

### 8.5 kb_document_v1

```bash
SRC=kb_document_v1
DST=kb_document_v1_v2
WRITE_ALIAS=kb_document_v1_write
```

当前 count 为 0。

建议先确认业务是否仍需要 `kb_document_v1`：

- 如果仍作为兜底索引：创建 `kb_document_v1_v2` 并切 alias。
- 如果已废弃：从 `kb_document` read alias 中移除 `kb_document_v1`，并保留旧索引用于审计。

## 9. 回滚方案

每个索引独立回滚。

以 `kb_document_public` 为例：

```json
{
  "actions": [
    {
      "remove": {
        "index": "kb_document_public_v2",
        "alias": "kb_document"
      }
    },
    {
      "remove": {
        "index": "kb_document_public_v2",
        "alias": "kb_document_public_write"
      }
    },
    {
      "add": {
        "index": "kb_document_public",
        "alias": "kb_document"
      }
    },
    {
      "add": {
        "index": "kb_document_public",
        "alias": "kb_document_public_write",
        "is_write_index": true
      }
    }
  ]
}
```

执行方式：

```bash
docker exec "$AI_CONTAINER" sh -c '
curl -s -X POST "$ES_HOST/_aliases" \
  -H "Content-Type: application/json" \
  -d @/app/docs/test_reports/rollback_public_alias.json
'
```

回滚后校验：

- `kb_document` alias 下当前业务索引 count 恢复为旧索引数量。
- 写 alias 指向旧索引。
- 应用检索可正常返回。

## 10. 风险清单与处理

### 10.1 Index Template 自动挂读 alias

现有 `kb_document_template` 会让新建 `kb_document_*` 自动挂上 `kb_document` alias。

已在迁移工具中处理：

- 未传 `--switch-alias` 时，脚本会自动移除目标 v2 上继承的 `kb_document` alias。

迁移后仍需人工校验：

```bash
curl -s "$ES_HOST/_cat/aliases/kb_document*?h=alias,index,is_write_index&format=json"
```

### 10.2 迁移期间有新写入

风险：

- v2 漏数据。

处理：

- 迁移单个索引前冻结该索引写入。
- alias 切换前再执行一次全量迁移命令，利用相同 `_id` 幂等覆盖。

### 10.3 权限 token 缺失

风险：

- 误放权。

处理：

- 脚本在 `acl_tokens` 缺失时写入 `_NO_ACCESS`。
- 迁移后统计 `_NO_ACCESS` 数量，人工确认是否合理。

### 10.4 空索引是否需要迁移

建议迁移。

原因：

- 空索引的旧 mapping 仍可能污染未来写入。
- 切到 v2 后，未来新写入会天然使用正确 mapping。

### 10.5 分片和副本

当前默认：

```text
shards = 1
replicas = 0
```

生产建议根据容量设置：

```bash
--shards 3 --replicas 1
```

注意：

- 分片数只能在新建索引时确定。
- 已有索引不能原地改主分片数。

## 11. 最终验收标准

全量完成后应满足：

1. 所有仍活跃的 `kb_document_*` 读写 alias 均指向 v2 索引。
2. 所有 v2 索引：
   - `acl_tokens = keyword`
   - `source_index = keyword`
   - `index_code = keyword`
   - `owner_unit_code = keyword`
   - `visible_unit_codes = keyword`
   - `permission_version = long`
3. 每个 v2 索引 count 与旧索引一致。
4. `kb_document` alias 下按 `source_index` 查询不出现双倍数据。
5. 权限 token 查询能命中授权数据。
6. 未授权 token 查询不能命中受限数据。
7. Java 检索链路和 AI 服务检索链路均通过回归测试。

## 12. 建议执行批次

| 批次 | 索引 | 操作 |
| --- | --- | --- |
| 已完成 | `kb_document_news` | 已迁移并切换到 `kb_document_news_v2` |
| 第 1 批 | `kb_document_public` | 迁移、校验、切 alias |
| 第 2 批 | `kb_document_official` | 迁移、校验、切 alias |
| 第 3 批 | `kb_document_law` | 空索引 v2 创建与 alias 切换 |
| 第 4 批 | `kb_document_notice` | 空索引 v2 创建与 alias 切换 |
| 第 5 批 | `kb_document_v1` | 确认保留或移出 alias |

